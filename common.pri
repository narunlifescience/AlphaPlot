# options common to all modules
CONFIG += debug
QT += xml

# modules to be linked in
# this will be handy for modules that require additional dependencies
MODULES = table notes

MOC_DIR        = ../tmp
OBJECTS_DIR    = ../tmp
RCC_DIR			= ../tmp
UI_DIR			= ../tmp
