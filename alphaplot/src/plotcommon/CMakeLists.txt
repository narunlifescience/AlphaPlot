set( current_dir "plotcommon" )

set( HEADERS ${HEADERS}
  "${current_dir}/widgets/ImageExportDialog.h"
  PARENT_SCOPE
  )
set( SRCS ${SRCS}
  "${current_dir}/widgets/ImageExportDialog.cpp"
  PARENT_SCOPE
  )
